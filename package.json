{
    "name": "girocode-ts",
    "version": "1.0.0",
    "scripts": {
        "build": "tsc && cp -r src/* dist/ && find dist -name '*.ts' -delete"
    },
    "devDependencies": {
        "typescript": "^5.0.0"
    }
}