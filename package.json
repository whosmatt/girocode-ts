{
    "name": "girocode-ts",
    "version": "1.0.0",
    "scripts": {
        "build": "tsc && cp -r src/* dist/ && find dist -name '*.ts' -delete && printf '\\n// build:%s\\n' \"$(date -u +%Y-%m-%dT%H:%M:%SZ)\" >> dist/sw.js"
    },
    "devDependencies": {
        "typescript": "^5.0.0"
    }
}